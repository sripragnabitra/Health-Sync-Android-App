# Add project-specific ProGuard rules here.
# minifyEnabled is false by default for this project (see app/build.gradle.kts);
# flip it on for a real release build and revisit kotlinx.serialization /
# Retrofit-specific keep rules at that point.
