NODE_ENV=development
PORT=4000

# Local Postgres, or the one from docker-compose.yml (see README)
DATABASE_URL="postgresql://healthsync:healthsync@localhost:5432/healthsync?schema=public"

# Generate something real: `openssl rand -base64 32`
JWT_SECRET="81d3625d2004457d7192da0fbfe9402717a201a72d62d9e438fcc910e5d4ba08"
JWT_EXPIRES_IN="7d"

# Must match the web dashboard's origin in dev
CORS_ORIGIN="http://localhost:3000"
